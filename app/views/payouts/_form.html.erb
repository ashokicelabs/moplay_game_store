<%= form_for @payout, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :id, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :id, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :publisher_id, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :publisher_id, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :amount_paid, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :amount_paid, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  payouts_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
